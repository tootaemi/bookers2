<% if flash[:notice] %>
<p id="notice">
 <%= flash[:notice] %> </p>
   <% end %>


<%= render 'users/list', users: @user,book: @book %>





<div>

<tr>

      <h2>Book detail</h2>
      <table class='table table-hover table-inverse'>

  <tbody>
  <% @books.each do |book| %>

  <% end %>


  <td><%=link_to books_path(@book.id) %></td>


  <td><%= @book.body %></td>
  <td><%= link_to "Show", book_path(@books)%>
  </td>
  <td>
  <%= link_to "Edit", edit_book_path(@books)%>
  </td>
  <td>
    <%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "Are you sure?" %>

  </td>
  </tr>



</tbody>
</table>
</div>


